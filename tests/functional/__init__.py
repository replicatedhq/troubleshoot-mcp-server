"""
Functional tests for MCP protocol validation.

These tests verify that the server works correctly through the MCP protocol layer,
rather than testing functions directly. This ensures protocol compatibility is maintained.
"""
